# 多智能体框架设计文档

## 一、总体思路

需要一个编排者（Orchestrator）负责将复杂任务分解成子任务，然后将子任务分发给 Worker
（可以是 ReAct Agent，可以是有特殊工具的 LLM，也可以是其他框架的 Agent），Worker 完成
任务后将信息回传给编排者，编排者汇总结果得出答案。

```
                    ┌─ Worker A (ReAct + 搜索工具)
用户 ──► Orchestrator ──┼─ Worker B (ReAct + 代码工具)
                    └─ Worker C (ReAct + 分析工具)
```

---

## 二、四种经典多智能体模式

| 模式                         | 特点                                       | 适用场景                |
| ---------------------------- | ------------------------------------------ | ----------------------- |
| **Orchestrator-Worker**      | 一个编排者拆解任务 → 分派 Worker → 汇总    | 复杂任务分解（本项目采用） |
| **Hierarchical**             | 多层管理，Manager → Sub-manager → Worker   | 超大型任务、团队管理    |
| **Peer-to-Peer**             | 平等对话，辩论 / 投票决定                  | 代码审查、方案论证      |
| **Sequential Pipeline**      | A 输出 → B 输入 → C 输入，流水线           | 翻译 → 校对 → 排版      |

---

## 三、编排者设计（核心问答汇总）

### 3.1 Prompt 设计——两个独立 Prompt

编排者需要 **两个 Prompt**，在不同阶段使用：

**Prompt A：任务拆解（Plan）**

```
你是一个任务编排者，负责将复杂任务拆解成子任务。

【可用 Worker】
- 搜索专家：擅长网络搜索、信息检索、资料收集
- 代码专家：擅长编写代码、调试、数据分析
- 写作专家：擅长撰写报告、润色文字

【规则】
1. 严禁直接回答用户的问题
2. 只输出子任务列表
3. 每个子任务必须指定分配给哪个 Worker
4. 子任务之间有依赖关系时，标注 depends_on

【输出格式】严格按以下 JSON 输出：
{
  "subtasks": [
    {"id": 1, "desc": "...", "worker": "搜索专家", "depends_on": []},
    {"id": 2, "desc": "...", "worker": "分析专家", "depends_on": [1]}
  ]
}
```

**Prompt B：汇总答案（Summarize）**

```
你是一个任务编排者。以下是各 Worker 完成子任务的结果，请基于这些结果回答用户问题。

【用户原始问题】
{original_question}

【子任务执行结果】
{worker_results}

请综合以上信息，给出完整、准确的回答。
```

**为什么分两个 Prompt？** 拆解任务和汇总信息是两种完全不同的思维模式，拆解需要结构化规划能力，汇总需要阅读理解+综合输出能力。拆解阶段的 Prompt 禁止模型直接回答问题，防止它跳过 Worker 自己瞎编。

### 3.2 Worker 能力声明——类比工具列表

**方案：在编排者 Prompt 中列出 Worker 能力列表，让 LLM 决定路由。**

这与 ReAct Agent 使用工具列表的逻辑完全一致：

```python
# ReActAgent:     tools    → LLM 决定调用哪个工具
# Orchestrator:   workers  → LLM 决定分派给哪个 Worker
```

编排者产出 `[{"id": 1, "desc": "...", "worker": "搜索专家"}, ...]`，**代码零判断，只做路由转发**：

```python
for task in plan["subtasks"]:
    worker = self.worker_map[task["worker"]]   # 按标识转发
    results[task["id"]] = await worker.reply(task["desc"])
```

这是最简单、最灵活的方案。如果 Worker 数量很多（>10 个），可改为让 LLM 用自然语言描述需求，代码做语义匹配。初期 3-5 个 Worker 时，直接在 Prompt 里列出即可。

### 3.3 任务分发——按依赖关系并行+串行组合

**三种策略不互斥，应组合使用：**

编排者产出的 TaskPlan 带有 `depends_on` 字段，形成一个 **DAG（有向无环图）**：

```
子任务1: 搜索公司信息 [搜索Worker]  depends_on: []       ──┐
子任务2: 搜索市场数据 [搜索Worker]  depends_on: []       ──┤ 批次1：并行执行
子任务3: 分析财报数据 [分析Worker]  depends_on: [1]      ──┐
子任务4: 分析市场数据 [分析Worker]  depends_on: [2]      ──┤ 批次2：等批次1完成
子任务5: 生成最终报告 [写作Worker]  depends_on: [3, 4]   ──┘ 批次3：等批次2完成
```

调度循环伪代码：

```python
async def execute_plan(plan, workers):
    results = {}
    remaining = list(plan["subtasks"])

    while remaining:
        # 找出所有依赖已满足的任务
        ready = [t for t in remaining
                 if all(dep in results for dep in t["depends_on"])]
        if not ready:
            raise RuntimeError("检测到循环依赖或缺失依赖")

        # 同一批次内并行执行
        async with asyncio.TaskGroup() as tg:
            futures = {}
            for task in ready:
                worker = workers[task["worker"]]
                futures[task["id"]] = tg.create_task(
                    worker.reply(task["desc"])
                )

        for task in ready:
            results[task["id"]] = await futures[task["id"]]
            remaining.remove(task)

    return results
```

### 3.4 结果汇总时机——全部等齐再汇总

**推荐：等所有子任务完成后一次性汇总。**

| 方式       | 优点             | 缺点                       |
| ---------- | ---------------- | -------------------------- |
| **全部等齐** | 编排者有全局视角 | 无法中途纠偏               |
| 分批交付   | 可以中途纠偏     | 多次 LLM 调用，成本高      |

**实际推荐折中方案：** 带依赖关系的批次是天然的汇总节点——每完成一个批次、且下一批次需要依赖当前结果才能决定后续策略时，做一次中间汇总。最终批次完成后做一次总汇总。汇总阶段切换到 Prompt B。

### 3.5 层级编排（Hierarchical）

Worker 可以是另一个 OrchestratorAgent，形成树状任务分发网络：

```
                    ┌─ 搜索Worker
CEO编排者 ── 研究总监编排者 ──┼─ 分析Worker
                    └─ 写作Worker
          │
          └── 代码总监编排者 ──┬─ 前端Worker
                              └─ 后端Worker
```

**核心权衡：** 编排者烧的是计划+汇总的 token，Worker 烧的是执行的 token。每多一层编排就多一次"理解→拆解→汇总"的 LLM 调用。

**实践建议：2 层上限。** 三层及以上只在子领域差异极大的场景才值得（如同时需要法律分析+代码开发+多语翻译），否则延迟和成本不划算。

**Worker 为什么一般是 ReAct？** Worker 拿到的是一个明确的子任务 + 专属工具，就是一个标准的单步推理循环。不需要再做任务拆解——再拆就过度工程化了。

---

## 四、实现中可能遇到的问题 & 解决方案

### 4.1 LLM 输出格式不可靠

**问题：** JSON 解析失败（缺少括号、多打文字、格式错误）。

**方案：**
- 强制 JSON 输出：`response_format={"type": "json_object"}` 或 `tool_choice` 约束
- 解析失败时自动重试，在重试 Prompt 中附上解析错误信息
- 三次重试仍失败 → 降级为串行依次发给 Worker

### 4.2 任务依赖循环

**问题：** LLM 产出 A depends_on B，B depends_on A。

**方案：**
- 代码端做拓扑排序检测循环依赖
- 检测到循环 → 返回错误给编排者要求重新规划

### 4.3 Worker 执行超时

**问题：** Worker 调用工具卡住（网络请求无响应等），整个编排流程阻塞。

**方案：**
- `asyncio.wait_for(worker.reply(...), timeout=N)` 包裹每个 Worker 调用
- 超时后重试一次，仍失败 → 返回错误摘要给编排者，由编排者决定是否降级

### 4.4 编排者上下文溢出

**问题：** 汇总阶段把所有 Worker 结果塞进一个 Prompt，超出 LLM context window。

**方案：**
- 对每个 Worker 结果做截断（只取前 N 个字符）或摘要压缩
- 用一个小模型先对每个结果做压缩，再交给编排者
- 设置 max_worker_results_length 参数

### 4.5 Worker 结果质量差

**问题：** Worker 返回"我做不到"、"我不知道"或完全无关的内容。

**方案：**
- 编排者在汇总 Prompt 中加入结果质量评判规则
- 发现质量差的 Worker 结果 → 重新派发（换个 Worker 或换个问法）
- 设置 max_retries_per_task 限制，防止无限重试卡死

### 4.6 错误传播与优雅降级

**问题：** 一个 Worker 失败 → 整个任务全废。

**方案：**
- 每个子任务失败时，不抛异常，而是将错误信息作为结果传给编排者
- 编排者汇总时提示它"部分子任务失败了，请基于已有信息给出最佳回答"
- 可选：编排者动态调整，用其他 Worker 补上失败的任务

### 4.7 并发控制

**问题：** 同批次 10 个 Worker 同时调 LLM，触发 API rate limit。

**方案：**
- `asyncio.Semaphore(limit)` 限制最大并发 Worker 数
- 每个 Worker 使用独立的 LLM 实例（注意 API key 的 rate limit）

### 4.8 动态重规划

**问题：** 执行到一半发现需要新的子任务（如 Worker 返回"我需要先知道 X 才能做这个"）。

**方案：**
- Worker 可以在返回结果中用特殊格式声明"需要前置信息"
- 编排者检测到 → 动态插入新子任务到计划中
- 也可以在每批次完成后让编排者评估是否需要增加/调整后续任务

### 4.9 消息路由混乱

**问题：** 多层编排时，消息传递方向容易混乱——用 Msg.name 做路由容易出错。

**方案：**
- 明确规定消息约定：子任务请求的 role="user"，Worker 结果的 role="user"
- 添加 metadata 字段标识消息类型：`{"msg_type": "task", "task_id": 1}` 或 `{"msg_type": "result", "task_id": 1}`
- Worker 间不允许直接通信，必须通过编排者转发（避免网状通信）

### 4.10 状态持久化与恢复

**问题：** 编排流程执行到一半 Crash 了，需要能从断点恢复。

**方案：**
- 将 TaskPlan + results 持久化到 JSON 文件
- 每次子任务完成立刻写入磁盘
- 启动时检测未完成的 Plan，从断点继续执行

### 4.11 Worker 工具冲突

**问题：** 多个 Worker 同时调用 `write_file` 写同一个文件。

**方案：**
- 给每个 Worker 分配独立的工作目录（sandbox）
- 或用文件锁 + 写入前检查
- 编排者约束：同一文件不分配给多个 Worker

### 4.12 调试困难

**问题：** 多智能体行为链太长，出问题难以定位。

**方案：**
- 统一日志格式：`[时间] [编排者/Woker-名称] 动作：内容`
- 每个子任务记录：输入 → Worker 名称 → 工具调用链 → 输出
- 支持单步调试模式（逐步确认后再执行下一个 Worker）

### 4.13 Token 成本爆炸

**问题：** 深层级编排 + 多次汇总 + 重试 = Token 账单惊人。

**方案：**
- 限制最大层级为 2
- 限制子任务数量上限（如 10 个）
- 用更便宜的模型做 Worker（如编排者用强模型，Worker 用弱模型）
- 汇总阶段压缩 Worker 结果（截断或摘要）

### 4.14 任务粒度控制

**问题：** LLM 拆出来的子任务太粗（基本等于原任务）或太细（100 个子任务）。

**方案：**
- Prompt 中约束子任务数量范围（如 "3-8 个子任务"）
- 代码端检查：少于 2 个 → 提示不够细；超过 N 个 → 要求合并

### 4.15 Worker 能力声明与 LLM 路由不匹配

**问题：** LLM 选择了不合适的 Worker，或编造了一个不存在的 Worker 名称。

**方案：**
- 代码端验证 `task["worker"]` 是否在 `worker_map` 中
- 不匹配 → 返回错误，让编排者重新选择
- 用每个 Worker 的能力描述做一次快速语义匹配，自动修正明显的选择错误

---

## 五、代码结构建议

基于现有代码（`src/`），建议新增以下文件：

```
src/
├── orchestrator.py    # OrchestratorAgent 类
├── worker_registry.py  # Worker 注册管理（类似 Toolkit）
├── scheduler.py        # 任务调度器（DAG 解析 + 并发执行）
├── run_multi.py        # 多智能体入口
├── loop_agent.py       # 保留（AgentBase, UserAgent, ReActAgent）
├── memory.py           # 保留
├── message.py          # 保留（可能需要扩展 msg_type 字段）
├── llm.py              # 保留
├── tools.py            # 保留
├── formatter.py        # 保留
└── llm_config.py       # 保留
```

### OrchestratorAgent 核心结构

```python
class OrchestratorAgent(AgentBase):
    def __init__(
        self,
        name: str,
        plan_prompt: str,        # Prompt A：任务拆解
        summarize_prompt: str,   # Prompt B：结果汇总
        model: ChatModelBase,    # 用于拆解 + 汇总的 LLM
        formatter: FormatterBase,
        workers: dict[str, AgentBase],  # Worker 映射表
        memory: MemoryBase,      # 编排者自己的记忆（存计划和结果）
        max_subtasks: int = 10,  # 最大子任务数
        max_retries: int = 2,    # 单任务最大重试次数
        timeout: int = 120,      # 单 Worker 超时（秒）
    ):
        ...

    async def reply(self, msg: Msg) -> Msg:
        # 1. 调用 LLM 拆解任务（Prompt A）
        plan = await self._plan(msg)

        # 2. 按 DAG 执行任务（scheduler 调度）
        results = await self._execute_plan(plan)

        # 3. 调用 LLM 汇总结果（Prompt B）
        answer = await self._summarize(msg, results)

        return answer
```

### 与现有类的复用关系

| 现有类        | 复用方式                                   |
| ------------- | ------------------------------------------ |
| `AgentBase`   | OrchestratorAgent 和 Worker 都继承它       |
| `ReActAgent`  | 直接作为 Worker 使用                        |
| `Toolkit`     | 每个 Worker 可拥有不同的 Toolkit 实例        |
| `Msg`         | Agent 间通信载体，metadata 扩展 msg_type     |
| `MemoryBase`  | 编排者和 Worker 各自保有独立的 Memory 实例   |
| `OpenAIChatModel` | 编排者和 Worker 可共用或各自使用不同模型  |
