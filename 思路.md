- # 简易 AI Agent 运行逻辑

  ## 一、核心运行逻辑

  1. 核心定义

     Agent 是接收用户总目标后，通过「推理→决策→工具调用→迭代」自动完成目标的程序，核心遵循 

     ReAct 隐式规划（思考 Thought + 行动 Action），不提前生成固定计划，动态分步执行。

  2. LLM 输出两种固定形态

     - 自然语言：推理中间步骤、子目标、最终答案
     - 结构化 JSON：工具调用请求（依赖模型原生 Function Calling / 特制 Prompt 约束格式）

  3. 核心闭环

     总目标 → LLM 推理 / 决策 → 判断是否调用工具 → 执行工具→ 工具结果回传 → 迭代推理 → 达成目标终止

  ## 二、Agent 必备上下文（完整修订版，无遗漏）

  上下文是 Agent 正常运行的核心，必须按固定顺序拼接，包含 6 类关键信息：

  1. **系统提示（System Prompt）**：定义 Agent 角色、工具使用规则、输出格式、终止条件（核心前提）
  2. **用户输入**：原始总目标
  3. **历史对话**：LLM 的推理过程、子目标、历史交互记录
  4. **工具清单**：所有可调用工具的名称、功能描述、参数格式
  5. **工具执行结果**：工具调用后的返回数据（必须标记`工具结果`角色，与调用配对）
  6. **终止信号**：模型输出自然语言最终答案时，触发循环结束

  ## 三、核心模块划分（最终修订细化版，极简可手写）

  保留你原有的 4 个核心模块，**仅补充 2 个极简必备模块**，明确每个文件的唯一职责，无冗余设计，完全适配手写简易 Agent：

  表格

  | 模块文件             | 核心职责（修订细化版） | 简易实现要求                                                 |
  | :------------------- | :--------------------- | :----------------------------------------------------------- |
  | `loop_agent.py`      | **Agent 主循环核心**   | 1. 控制「推理→调用工具→迭代」的死循环2. 解析 LLM 输出（区分自然语言 / 工具调用）3. 判断终止条件（最终答案→退出循环）4. 处理基础异常（工具失败、API 报错） |
  | `memory.py`          | **上下文记忆管理**     | 1. 存储所有上下文消息（系统提示、用户输入、推理、工具结果）2. 基础上下文裁剪（防止超 token 限制，简易版用滑动窗口）3. 提供「添加消息 / 获取完整上下文」接口 |
  | `message.py`         | **消息格式标准化**     | 1. 定义消息类型（系统、用户、AI 推理、工具结果）2. 统一 LLM 输入 / 输出格式（适配模型 API）3. 解析工具调用 JSON（提取工具名、参数） |
  | `tools.py`           | **工具定义与执行**     | 1. 注册所有可用工具（如搜索、计算器、文件读取）2. 工具参数验证3. 执行工具并返回格式化结果（成功 / 失败信息） |
  | `llm.py`（新增必备） | **LLM 接口封装**       | 1. 封装大模型 API 调用（屏蔽底层模型差异）2. 传入上下文，返回 LLM 原始输出3. 简易重试 / 超时处理 |
  | `run.py`（新增必备） | **程序入口**           | 1. 初始化所有模块（记忆、工具、LLM、Agent）2. 接收用户输入，启动 Agent 主循环3. 输出最终结果 |

  ## 四、核心执行流程（修订版，一步一执行）

  1. 启动`run.py`，输入用户总目标

  2. `memory.py`初始化上下文，插入**系统提示**

  3. ```
     loop_agent.py
     ```

     启动主循环：

     - 从`memory.py`获取完整上下文

     - 调用`llm.py`请求模型推理

     - 交给message.py解析模型输出：

       ✅ 为自然语言

       ✅ 为工具调用 JSON：提取工具名 + 参数，添加提供提交答案的工具，调用这个工具时结束循环调用

  4. 调用`tools.py`执行对应工具，将结果存入`memory.py`

  5. 迭代步骤 3，直到触发终止条件